[pytest]
asyncio_mode = auto
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
# Golden tests hit the real Gemini API — excluded from the default run.
# Run them explicitly: pytest tests/golden/ -v
addopts = -v --tb=short --ignore=tests/golden
